import 'package:flutter/material.dart';

class logo_big extends StatelessWidget {
  const logo_big({super.key});

  @override
  Widget build(BuildContext context) {
    return const Image(image: AssetImage('lib/assets/logo/logo_big.png'));
  }
}