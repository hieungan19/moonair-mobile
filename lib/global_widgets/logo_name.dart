import 'package:flutter/material.dart';

class logo_name extends StatelessWidget {
  const logo_name({super.key});

  @override
  Widget build(BuildContext context) {
    return const Image(image: AssetImage('lib/assets/logo/logo_name.png'));
  }
}